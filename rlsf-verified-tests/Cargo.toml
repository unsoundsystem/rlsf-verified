[package]
name = "rlsf-verified-tests"
version = "0.1.0"
edition = "2021"

[dependencies]
rlsf-verified = { path = "..", features = ["std"] }

[dev-dependencies]
quickcheck = "1"
quickcheck_macros = "1"
